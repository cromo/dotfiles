" Change the way Vim looks.
syntax on

" Change the way the editor behaves when not editing.
set scrolloff=7

" Search options
set ignorecase
set smartcase
set incsearch
set hlsearch

" Change the way the editor behaves when editing.
set cindent

" Use two space indents
set softtabstop=2 shiftwidth=2 expandtab
